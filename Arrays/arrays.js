function shouldEqualTo(arr1) {
    return arr1;
}

function fruits(fruits) {
    return fruits;
}

function colors(colors) {
    return colors;
}
    
module.exports = {
    shouldEqualTo,
    fruits,
    colors
}